<h3>These are the Musts tagged as <%=h @tag %></h3>

<% unless @musts.blank? %>

<ul class="musts">
  <% @musts.each do |must| %>
  	<%= render :partial => "musts/must_item", :locals => {:must => must} %>
  <% end %>
</ul>

<%= will_paginate @musts %>

<% end %>
