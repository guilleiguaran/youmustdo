<% if current_user %>
<% if current_user.has_favorite?(must)%>
<li><%= link_to_remote "", { :url => must_unfavorite_path(must) }, :class => "unbookmark" -%></li>
<% else %>
<li><%= link_to_remote "", { :url => must_favorite_path(must) }, :class => "bookmark" -%></li>
<% end %>
<% end %>